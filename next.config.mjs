/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["platform-lookaside.fbsbx.com", "lh3.googleusercontent.com", "s2.coinmarketcap.com", "crypto.snapi.dev"],
  },
};

export default nextConfig;
